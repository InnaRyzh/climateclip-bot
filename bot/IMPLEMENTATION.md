# Реализация Telegram-бота

## Что реализовано

### ✅ Базовая структура
- Telegram-бот на `node-telegram-bot-api`
- Express сервер для health checks
- Управление состояниями пользователей
- Меню выбора шаблона

### ✅ Шаблон 1 (Grid 4x)
- Загрузка 4 видеофайлов
- Поддержка двух способов указания стран:
  - Все страны одной строкой через запятую
  - Отдельная страна после каждого видео
- Центральная дата/описание
- Рендеринг через Puppeteer

### ✅ Шаблон 2 (News)
- Загрузка 5 видеофайлов
- Страна, дата и 3 строки описания
- Рендеринг последовательности видео

### ✅ Инфраструктура
- Конфигурация для Railway
- Управление файлами (скачивание, очистка)
- Обработка ошибок

## Что нужно доделать

### 🔧 Улучшение рендеринга

1. **Полная интеграция существующей логики compositor.ts**
   - Сейчас используется упрощенная версия рендерера
   - Нужно адаптировать весь код из `services/compositor.ts` для работы в Puppeteer
   - Включить все оверлеи (логотип, дата, страны, тексты)

2. **Автоматический выбор лучших 5 секунд**
   - Для новостного шаблона нужно анализировать видео
   - Варианты реализации:
     - Использовать первые 5 секунд (простой вариант)
     - Анализ яркости/контраста для выбора лучшего кадра
     - Использовать ML модель для определения интересных моментов

3. **Оптимизация производительности**
   - Кэширование видео
   - Параллельная обработка
   - Оптимизация памяти для больших видео

### 📝 Улучшение UX

1. **Прогресс рендеринга**
   - Отправка промежуточных сообщений пользователю
   - Показ процента выполнения

2. **Валидация данных**
   - Проверка формата видео
   - Проверка размера файлов
   - Предупреждения о длительности

3. **Обработка ошибок**
   - Более понятные сообщения об ошибках
   - Retry механизм для неудачных рендеров

## Следующие шаги

1. **Протестировать локально:**
   ```bash
   cd bot
   npm install
   # Создать .env с BOT_TOKEN
   npm run dev
   ```

2. **Улучшить рендерер:**
   - Скопировать полную логику из `services/compositor.ts`
   - Адаптировать для работы в Puppeteer
   - Добавить все функции отрисовки (логотип, оверлеи и т.д.)

3. **Добавить выбор лучших 5 секунд:**
   - Реализовать анализ видео через ffmpeg
   - Или использовать первые 5 секунд как fallback

4. **Деплой на Railway:**
   - Создать проект на Railway
   - Добавить переменную BOT_TOKEN
   - Задеплоить код

## Структура файлов

```
bot/
├── server.ts              # Основной сервер и логика бота
├── videoRenderer.ts       # Рендеринг видео через Puppeteer
├── fileManager.ts         # Управление файлами
├── compositorAdapter.ts   # Адаптер (можно удалить, если не используется)
├── package.json           # Зависимости
├── tsconfig.json          # TypeScript конфигурация
├── railway.json           # Конфигурация Railway
├── nixpacks.toml          # Альтернативная конфигурация Railway
├── SETUP.md               # Инструкция по настройке
└── IMPLEMENTATION.md      # Этот файл
```

## Зависимости

- `node-telegram-bot-api` - Telegram Bot API
- `express` - HTTP сервер
- `puppeteer` - Headless браузер для рендеринга
- `dotenv` - Переменные окружения
- `fluent-ffmpeg` - Обработка видео (опционально, для анализа)

## Примечания

- Рендеринг видео требует много ресурсов (CPU, RAM)
- На Railway может потребоваться план с большим объемом памяти
- Рекомендуется использовать кэширование для часто используемых видео
- Для продакшена можно рассмотреть очередь задач (например, Bull/BullMQ)


